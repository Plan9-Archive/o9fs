- o9fs_{read,write,readdir} should use malloc (or pools) for buffers
- locking

known bugs:
cannot unmount without forcing
connect doesn't check if socket is connected to send data
deadlock in VOP_OPEN when trying to acquire the readlock

REGEN openbsd-headers.diff